# Univ Chrome Extensions Data Comments

Originally derived from the `univ-crx.jsonc` comments.

- `extensions`: 拡張機能リスト

## Fields (拡張機能のフィールド)
- `title`       : 拡張機能名
- `description` : 説明文
- `github`      : GitHubリポジトリURL
- `tags`        : タグ配列
- `status`      : "active" | "archived" | "wip"
